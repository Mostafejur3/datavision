import { ArrowLeft, ArrowUpRight, BarChart3, Brain, Check, Database, LayoutDashboard, Menu, MessageCircle, Search, Sparkles, TrendingUp, X } from 'lucide-react'
import { useState } from 'react'
import './marketing.css'
import './pages.css'

const services = [
  ['Data Analysis & Insight', 'Find the signal inside your operational data.', BarChart3],
  ['Interactive Dashboards', 'Give every decision-maker a live view of performance.', LayoutDashboard],
  ['Python & ML Modeling', 'Forecast demand, risk, and growth with confidence.', Brain],
  ['Data Cleaning & ETL', 'Turn fragmented sources into a dependable data foundation.', Sparkles],
  ['BI Strategy', 'Align metrics, teams, and action around one source of truth.', TrendingUp],
  ['Market Research', 'See competitors clearly and move before the market does.', Database],
  ['Data Governance', 'Create reliable definitions, ownership, and access controls.', Check],
  ['Forecasting & Planning', 'Turn historical patterns into confident operating plans.', TrendingUp],
  ['Experimentation', 'Measure what works and learn faster than your market.', BarChart3],
  ['Executive Reporting', 'Give leadership a crisp view of the metrics that matter.', LayoutDashboard],
  ['Customer Analytics', 'Understand retention, segments, and lifetime value.', Brain],
  ['Automation & ETL', 'Remove manual reporting work with resilient pipelines.', Sparkles],
] as const
const featured = [['Revenue Intelligence', 'A live command center for your most important growth metrics.', '$12k–$28k'], ['Predictive Demand Model', 'Know what your customers need before they ask.', '$18k–$42k'], ['Executive KPI Suite', 'One calm, credible view for every leadership decision.', '$8k–$20k'], ['Data Foundation Sprint', 'Clean pipelines and a source of truth your team can trust.', '$6k–$16k']]
const caseStudies = [['Retail growth engine', '+32%', 'Revenue lift'], ['Inventory intelligence', '$2.3M', 'Annual savings'], ['Fintech forecasting', '94%', 'Forecast accuracy']]
const testimonials = [['“DataVision gave our leadership team a shared language for growth. We moved from weekly debates to daily decisions.”', 'Maya Chen', 'COO, TechRetail'], ['“The dashboard paid for itself in the first quarter. More importantly, our team finally trusts the numbers.”', 'Arjun Mehta', 'VP Operations, GlobalLogistics'], ['“They combine strategic thinking with serious technical depth. It feels like adding a senior analytics team overnight.”', 'Elena Brooks', 'Founder, FinanceFirst']]
const tech = ['Python', 'Pandas', 'NumPy', 'Scikit-learn', 'TensorFlow', 'PyTorch', 'Power BI', 'Tableau', 'Looker', 'SQL', 'PostgreSQL', 'BigQuery', 'Snowflake', 'dbt', 'Apache Spark', 'Airflow', 'Excel', 'Google Sheets', 'R', 'Plotly', 'D3.js', 'Streamlit', 'Supabase']
const stats = [['150+', 'Projects delivered'], ['98%', 'Client satisfaction'], ['$12M+', 'Client revenue generated'], ['12+', 'Industries served']]

function App() {
  const path = window.location.pathname
  if (path !== '/') return <PageRouter path={path} />
  return <Home />
}

function SiteNav() { const [open, setOpen] = useState(false); return <nav className="nav shell"><a className="brand" href="/"><span className="brand-mark">D</span> DataVision</a><div className={`nav-links ${open ? 'open' : ''}`}><a href="/services">Services</a><a href="/portfolio">Work</a><a href="/about">About</a><a href="/blog">Insights</a><a href="/docs">Docs</a></div><a className="button button-small" href="/contact">Book a free audit <ArrowUpRight size={16} /></a><button className="menu-button" onClick={() => setOpen(!open)} aria-label="Toggle navigation">{open ? <X /> : <Menu />}</button></nav> }
function PageRouter({ path }: { path: string }) { if (path === '/services') return <Catalog />; if (path.startsWith('/services/')) return <ServiceDetail />; if (path === '/portfolio') return <Portfolio />; if (path === '/about') return <About />; if (path === '/contact') return <Contact />; if (path === '/blog') return <Blog />; if (path.startsWith('/blog/')) return <BlogPost />; if (path === '/docs') return <Docs />; return <NotFound /> }
function PageFrame({ children, title, eyebrow }: { children: React.ReactNode; title: string; eyebrow: string }) { return <><SiteNav /><header className="page-hero shell"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1></header>{children}<Footer /></> }
function SectionIntro({ eyebrow, title, copy }: { eyebrow: string; title: string; copy: string }) { return <div className="section-intro"><p className="eyebrow">{eyebrow}</p><h2>{title}</h2><p>{copy}</p></div> }
function Footer() { return <footer className="footer shell"><a className="brand" href="/"><span className="brand-mark">D</span> DataVision</a><span>Analytics that earns its keep.</span><span>© 2026 DataVision</span></footer> }
function Home() { const [testimonial, setTestimonial] = useState(0); return <main><SiteNav /><section id="top" className="hero shell"><div className="orb orb-one" /><div className="orb orb-two" /><div className="hero-copy"><div className="eyebrow"><span className="pulse" /> Trusted by 50+ data-driven companies</div><h1>Turn raw data into <em>decisive advantage.</em></h1><p className="hero-lede">We design dashboards, build ML models, and deliver insights that measurably grow revenue, cut costs, and eliminate guesswork—for teams that refuse to fly blind.</p><div className="actions"><a className="button" href="/contact">Book a free audit <ArrowUpRight size={18} /></a><a className="button button-ghost" href="/portfolio">See our work</a></div><div className="trust-strip"><span>★ 4.9 avg rating</span><span>98% retention</span><span>50+ projects</span><span>12+ industries</span></div></div><DashboardPreview /></section><section className="logo-bar"><div className="logo-track">{['TechRetail', 'MedCenter', 'GlobalLogistics', 'FinanceFirst', 'PropInvest', 'BrandBoost'].map((logo) => <span key={logo}>{logo}</span>)}</div></section><section id="services" className="section shell"><SectionIntro eyebrow="What we do" title="Everything you need to become data-driven" copy="From raw spreadsheets to revenue-driving dashboards, we handle the entire analytics lifecycle." /><div className="service-grid">{services.slice(0, 6).map(([title, copy, Icon]) => <article className="glass-card service-card" key={title}><div className="icon-tile"><Icon size={22} /></div><h3>{title}</h3><p>{copy}</p><a href="/services">Learn more <ArrowUpRight size={15} /></a></article>)}</div></section><section id="process" className="section section-muted"><div className="shell"><SectionIntro eyebrow="How we work" title="From chaos to clarity in four steps" copy="A focused, collaborative process that turns uncertainty into momentum." /><div className="process-grid">{['Discovery call', 'Data audit & strategy', 'Build & analyze', 'Deliver, train & support'].map((step, i) => <div className="process-step" key={step}><span>{String(i + 1).padStart(2, '0')}</span><h3>{step}</h3><p>{['We listen to the questions keeping you up at night.', 'We map sources, find gaps, and design the plan.', 'We clean, model, and visualize with weekly check-ins.', 'You get dashboards, docs, and 30-day support.'][i]}</p></div>)}</div></div></section><section className="section shell story-section"><div className="story-copy"><p className="eyebrow">Why you need this</p><h2>Data should remove friction—not create more of it.</h2><p>We build the calm, connected layer between your data and your next best decision.</p><ul>{['+32% average revenue lift after dashboard rollout', '−40% reduction in reporting time via automation', '$2.3M saved annually on inventory waste', '94% forecast accuracy with our ML models'].map((item) => <li key={item}><Check size={18} /> {item}</li>)}</ul></div><div className="signal-card"><div className="signal-orbit" /><div className="signal-center"><span>Signal</span><strong>↗</strong><small>from noise</small></div></div></section><section className="section section-muted"><div className="shell"><SectionIntro eyebrow="Featured services" title="Start with the question that matters most" copy="Focused engagements designed to create momentum quickly." /><div className="featured-grid">{featured.map(([title, copy, price], i) => <article className="featured-card" key={title}><span className="card-index">0{i + 1}</span><h3>{title}</h3><p>{copy}</p><strong>{price}</strong><a href="/contact">Explore engagement <ArrowUpRight size={15} /></a></article>)}</div></div></section><section id="portfolio" className="section shell"><SectionIntro eyebrow="Selected outcomes" title="The work speaks in numbers" copy="Replace fragmented reporting with useful intelligence." /><div className="case-grid">{caseStudies.map(([title, value, label], i) => <article className={`case-card case-${i + 1}`} key={title}><span>Case study / 0{i + 1}</span><strong>{value}</strong><h3>{title}</h3><p>{label}</p><a href="/portfolio">View outcome <ArrowUpRight size={15} /></a></article>)}</div></section><section className="section section-muted"><div className="shell"><SectionIntro eyebrow="Client perspective" title="Good analytics changes the conversation" copy="The best proof is what happens after the dashboard goes live." /><div className="testimonial-card"><div className="stars">★★★★★</div><blockquote>{testimonials[testimonial][0]}</blockquote><div className="testimonial-person"><span>{testimonials[testimonial][1].split(' ').map((n) => n[0]).join('')}</span><div><strong>{testimonials[testimonial][1]}</strong><small>{testimonials[testimonial][2]}</small></div></div><div className="testimonial-controls">{testimonials.map((_, i) => <button className={i === testimonial ? 'active' : ''} onClick={() => setTestimonial(i)} aria-label={`Show testimonial ${i + 1}`} key={i} />)}</div></div></div></section><section className="section shell"><SectionIntro eyebrow="Our toolkit" title="We speak fluent data" copy="The right tools matter. The right architecture matters more." /><div className="tech-grid">{tech.map((item, i) => <span key={item} style={{ animationDelay: `${i * 35}ms` }}>{item}</span>)}</div></section><section id="results" className="stats-section"><div className="shell"><p className="eyebrow">The DataVision effect</p><h2>Built to earn its keep.</h2><div className="stats-grid">{stats.map(([value, label]) => <div className="stat" key={label}><strong>{value}</strong><span>{label}</span></div>)}</div></div></section><CTA /><Footer /><a className="chat-fab" href="/contact" aria-label="Contact DataVision"><MessageCircle size={20} /></a></main> }
function CTA() { return <section id="contact" className="cta shell"><div><p className="eyebrow">Ready when you are</p><h2>Make data your unfair advantage.</h2><p>Book a free 30-minute audit. We’ll show you where you’re losing money—and how to fix it.</p></div><a className="button" href="/contact">Book free audit <ArrowUpRight size={18} /></a></section> }
function DashboardPreview() { return <div className="dashboard-wrap"><div className="dashboard-card"><div className="dashboard-head"><span>Revenue intelligence</span><span className="live"><i /> Live</span></div><div className="kpi-row"><div><small>Net revenue</small><strong>$284,920</strong><span className="positive">↗ 32.8%</span></div><div><small>Forecast confidence</small><strong>94.2%</strong><span className="positive">↗ 8.4%</span></div></div><div className="chart"><div className="chart-grid" /><svg viewBox="0 0 420 150" preserveAspectRatio="none"><path d="M0 130 C35 120 45 90 75 102 S120 118 150 82 S195 105 225 64 S270 72 300 45 S350 50 375 18 S405 28 420 4" fill="none" stroke="url(#line)" strokeWidth="4" strokeLinecap="round" /><defs><linearGradient id="line" x1="0" x2="1"><stop stopColor="#22d3ee" /><stop offset="1" stopColor="#6366f1" /></linearGradient></defs></svg></div><div className="dashboard-foot"><span>Performance trend</span><span>Last 30 days <ArrowUpRight size={13} /></span></div></div></div> }
function Catalog() { const [query, setQuery] = useState(''); const filtered = services.filter(([name, copy]) => `${name} ${copy}`.toLowerCase().includes(query.toLowerCase())); return <PageFrame eyebrow="Capabilities" title="Services built around better decisions"><section className="section shell"><div className="catalog-toolbar"><div className="search-box"><Search size={18} /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search services" aria-label="Search services" /></div><div className="filter-pills"><button className="active">All services</button><button>Analytics</button><button>Engineering</button><button>Strategy</button></div></div><div className="package-tabs"><span>Engagement lens</span><button className="active">Basic</button><button>Standard</button><button>Premium</button></div><div className="service-grid catalog-grid">{filtered.map(([title, copy, Icon], i) => <article className="glass-card service-card" key={title}><div className="icon-tile"><Icon size={22} /></div><span className="service-number">{String(i + 1).padStart(2, '0')}</span><h3>{title}</h3><p>{copy}</p><a href={`/services/${title.toLowerCase().replaceAll(' ', '-')}`}>Explore service <ArrowUpRight size={15} /></a></article>)}</div>{filtered.length === 0 && <EmptyState text="No service matches that search." />}</section></PageFrame> }
function ServiceDetail() { const service = decodeURIComponent(window.location.pathname.split('/').pop() || 'service').replaceAll('-', ' '); return <PageFrame eyebrow="Service detail" title={service.replace(/\b\w/g, (c) => c.toUpperCase())}><section className="section shell detail-layout"><article><div className="detail-visual"><BarChart3 size={48} /><span>Built for clarity, speed, and measurable impact.</span></div><SectionIntro eyebrow="What you get" title="A decision system your team can trust" copy="We pair sharp business thinking with rigorous implementation to turn your biggest question into a useful, durable data product." /><div className="deliverable-grid">{['Discovery and KPI map', 'Clean, documented data model', 'Interactive executive view', 'Training and handover', '30-day optimization support', 'Clear next-step roadmap'].map((item) => <div key={item}><Check size={17} />{item}</div>)}</div><div className="comparison"><h2>From noise to next action</h2><div><div><span>Before</span><p>Manual reports, conflicting definitions, slow answers.</p></div><div><span>After</span><p>One trusted view, clear owners, faster decisions.</p></div></div></div><SectionIntro eyebrow="Our process" title="A focused path to first value" copy="Align, build, launch, and improve without losing momentum." /><div className="process-grid">{['Align', 'Model', 'Launch', 'Improve'].map((step, i) => <div className="process-step" key={step}><span>0{i + 1}</span><h3>{step}</h3><p>{['Define the decision.', 'Build the foundation.', 'Make insight visible.', 'Compound the result.'][i]}</p></div>)}</div><div className="faq"><h2>Frequently asked questions</h2>{['How quickly can we start?', 'Can you work with our existing tools?', 'What happens after launch?'].map((q) => <details key={q}><summary>{q}</summary><p>We tailor the engagement to your team, data maturity, and desired outcome. The first call turns that into a clear plan.</p></details>)}</div></article><aside className="pricing-card"><p className="eyebrow">Typical engagement</p><h2>From $6k</h2><p>Final scope is shaped around your goals, systems, and timeline.</p><a className="button" href="/contact">Start a conversation <ArrowUpRight size={17} /></a><a className="back-link" href="/services"><ArrowLeft size={15} /> Back to services</a></aside></section><div className="mobile-cta"><a className="button" href="/contact">Book your free audit <ArrowUpRight size={17} /></a></div></PageFrame> }
function Portfolio() { return <PageFrame eyebrow="Selected work" title="Outcomes worth sharing"><section className="section shell"><div className="case-grid portfolio-grid">{[...caseStudies, ['Healthcare operations', '−40%', 'Reporting time']].map(([title, value, label], i) => <article className={`case-card case-${(i % 3) + 1}`} key={title}><span>Case study / 0{i + 1}</span><strong>{value}</strong><h3>{title}</h3><p>{label}</p><a href="/contact">Discuss a similar outcome <ArrowUpRight size={15} /></a></article>)}</div><div className="results-banner"><p className="eyebrow">A pattern we see often</p><h2>Better visibility creates better velocity.</h2><p>Every engagement is designed to leave your team more capable than we found it.</p></div></section></PageFrame> }
function About() { return <PageFrame eyebrow="About DataVision" title="We make complexity useful"><section className="section shell about-grid"><div><SectionIntro eyebrow="Our belief" title="Clarity is a competitive advantage." copy="DataVision exists to help ambitious teams replace uncertainty with evidence. We are a small, senior, outcome-obsessed analytics partner." /><div className="values-grid">{[['01', 'Stay curious', 'Ask better questions before building anything.'], ['02', 'Make it useful', 'Insight only matters when it changes action.'], ['03', 'Leave a system', 'Build capability, not dependency.']].map(([n, t, c]) => <article key={n}><span>{n}</span><h3>{t}</h3><p>{c}</p></article>)}</div></div><div className="about-visual"><div><strong>50+</strong><span>teams helped</span></div><div><strong>12+</strong><span>industries explored</span></div><div><strong>1</strong><span>shared source of truth</span></div></div></section><CTA /></PageFrame> }
function Contact() { const [sent, setSent] = useState(false); return <PageFrame eyebrow="Start a conversation" title="Bring us the messy question"><section className="section shell contact-grid"><div><SectionIntro eyebrow="Free audit" title="Let’s find your highest-leverage opportunity." copy="Tell us what is unclear, slow, or expensive. We’ll reply with a useful first perspective—not a generic sales deck." /><div className="contact-points"><p><strong>hello@datavision.agency</strong><span>Typical reply within one business day</span></p><p><strong>30-minute audit</strong><span>No pressure, just a clear next step</span></p></div></div><form className="contact-form" onSubmit={(e) => { e.preventDefault(); setSent(true) }}>{sent ? <div className="success-state"><Check size={34} /><h2>Message received.</h2><p>We’ll be in touch with a thoughtful next step.</p></div> : <><label>Name<input required placeholder="Your name" /></label><label>Work email<input required type="email" placeholder="you@company.com" /></label><label>What should we improve?<textarea required rows={5} placeholder="Tell us about the question behind the request..." /></label><button className="button" type="submit">Send inquiry <ArrowUpRight size={17} /></button></>}</form></section></PageFrame> }
function Blog() { return <PageFrame eyebrow="Insights" title="Useful thinking for data-driven teams"><section className="section shell blog-grid">{['The dashboard is not the strategy', 'Five questions to ask before building an ML model', 'How to create a metric your whole team trusts', 'From spreadsheet sprawl to a data operating system'].map((title, i) => <article className="blog-card" key={title}><span>Data practice · 0{i + 1}</span><h2>{title}</h2><p>Practical notes on turning analysis into better decisions, healthier operations, and durable growth.</p><a href={`/blog/${i + 1}`}>Read article <ArrowUpRight size={15} /></a></article>)}</section></PageFrame> }
function BlogPost() { return <PageFrame eyebrow="Data practice · 8 min read" title="The dashboard is not the strategy"><article className="article shell"><p className="article-lede">A dashboard is a surface. The strategy is the operating rhythm, ownership, and decisions that happen because it exists.</p>{['Start with the decision', 'Design for the weekly moment', 'Make ownership visible'].map((heading) => <section key={heading}><h2>{heading}</h2><p>Good analytics begins with context. Name the decision, the person making it, and the cadence in which the answer needs to arrive. Then build only what makes that moment clearer.</p></section>)}<a className="back-link" href="/blog"><ArrowLeft size={15} /> Back to insights</a></article></PageFrame> }
function Docs() { return <PageFrame eyebrow="Documentation" title="A clearer way to work with DataVision"><section className="section shell docs-layout"><aside><a className="active" href="#start">Getting started</a><a href="#services-docs">Services</a><a href="#delivery">Delivery model</a><a href="#faq">FAQ</a><a href="#contact">Contact</a></aside><article><SectionIntro eyebrow="18-part guide" title="Everything in one calm place" copy="A practical overview of our services, engagements, delivery model, and handover process." />{['Start here', 'Choose the right engagement', 'Prepare your data', 'Understand the delivery rhythm', 'Review and approve milestones', 'Use your new operating system'].map((heading, i) => <section className="doc-section" id={i === 0 ? 'start' : undefined} key={heading}><span>0{i + 1}</span><h2>{heading}</h2><p>We document the why, what, and how so your team can move with confidence. Every step is designed to be transparent, collaborative, and easy to pick up.</p></section>)}</article></section></PageFrame> }
function NotFound() { return <PageFrame eyebrow="404" title="This page drifted into the dataset"><section className="section shell empty-page"><p>Let’s get you back to a clearer view.</p><a className="button" href="/">Return home <ArrowUpRight size={17} /></a></section></PageFrame> }
function EmptyState({ text }: { text: string }) { return <div className="empty-state"><Search size={24} /><p>{text}</p></div> }
export default App
